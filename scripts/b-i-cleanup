#!/bin/bash

set -e

## check if there is mounted-separated home partition. if true, unmoun
if cat /proc/mounts | grep -F " /target/home " > /dev/null; then
  umount /target/home
fi

umount /target
rm -rf /rofs
