language: c

before_script:
- sudo add-apt-repository ppa:vala-team/ppa -y
- sudo apt-get update -qq
- sudo apt-get install -qq gnome-common libglib2.0-dev libgtk-3-dev
- sudo apt-get install -qq libsqlite3-dev libvala-0.32-dev valac-0.32 gobject-introspection
- sudo apt-get install -qq libwebkitgtk-3.0-dev libparted0-dev libgee-dev
- sudo apt-get install -qq libglibmm-2.4-1c2a libsigc++-2.0-0c2a
- ./autogen.sh
- ./configure

script: make
